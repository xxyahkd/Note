import  包名.类1名    使当前所在的.java文件中可以仅使用类名访问该类，而不用使用完整类名  
                      如果不写全称，仅使用类名，默认只在当前包中寻找
private 修饰的成员仅是对内使用  在定义前加 如 private double buy = 3;
默认访问修饰符(就是不加修饰符) 只有在同一个包内可用
public 完全公开权限
protected 修饰的成员可以被同一个包中的类以及子类使用
对于类而言就是只有public 和默认